#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>
#include <stdlib.h>

/* FUNTION PROTOTYPES */
int _printf(const char *format, ...);
int _writechar(char c);
void print_string(char *str);
void print_number(int n);

#endif
